name: "PR Preview"
description: "PR preview helper for PRs and forks."
author: "open-resource-discovery"

inputs:
  fork:
    description: 'Set to "true" to enable forks support.'
    required: false
    default: "false"

  build-dir:
    description: "Directory containing the built preview for direct deploy from the current job."
    required: false
    default: "build"

  token:
    description: "Token used for API + deploy/cleanup (PAT recommended - falls back to GITHUB_TOKEN when not available)."
    required: false
    default: ""

  target-repo:
    description: "Owner/repo that will host the previews (deploy/cleanup)."
    required: false

  target-branch:
    description: "Branch to which previews are deployed."
    required: false
    default: "main"

  pages-base-url:
    description: 'Base URL used in the preview link (e.g. "https://<org>.github.io/<repo>").'
    required: false
    default: ""

  qr-code:
    description: 'Include an internally generated QR code in the PR comment. Set to "false" to disable.'
    required: false
    default: "true"

  comment-header:
    description: "Header used to identify the sticky PR comment."
    required: false
    default: "pr-preview"

  comment-title:
    description: "Title shown in the PR comment."
    required: false
    default: "PR Preview"

  comment-footer:
    description: "Footer (branding/ads) shown in the PR comment. Leave empty to hide."
    required: false
    default: "[open-resource-discovery/pr-preview-action](https://github.com/open-resource-discovery/pr-preview-action)"

  umbrella-dir:
    description: "Top-level directory inside the target repo to namespace previews."
    required: false
    default: ""

  wait-poll-attempts:
    description: "Maximum number of reachability check attempts."
    required: false
    default: "36"

  wait-poll-interval-seconds:
    description: "Seconds to wait between reachability check attempts."
    required: false
    default: "5"

  qr-wait-poll-attempts:
    description: "Maximum number of QR asset validation attempts."
    required: false
    default: "12"

  qr-wait-poll-interval-seconds:
    description: "Seconds to wait between QR asset validation attempts."
    required: false
    default: "10"

runs:
  using: "composite"
  steps:
    - name: Setup environment
      id: setup
      shell: bash
      run: |
        set -euo pipefail

        # Constants
        ARTIFACT_NAME="pr-preview"
        META_FILE="pr-preview-meta.json"

        EVENT_NAME="${{ github.event_name }}"
        REPO_NAME="${{ github.repository }}"
        DEPLOYMENT_ACTION="skip"  # deploy | upload | remove | worker | skip
        PR_TYPE="unknown"         # local | fork
        PR_NUMBER=""
        HEAD_REPO_FULL_NAME=""
        TARGET_REPO="${{ inputs.target-repo }}"
        TARGET_BRANCH="${{ inputs.target-branch }}"
        BUILD_DIR="${{ inputs.build-dir }}"
        PAGES_BASE_URL="${{ inputs.pages-base-url }}"
        QR_CODE_INPUT="${{ inputs.qr-code }}"
        QR_CODE_SUFFIX=""
        QR_CODE_URL=""
        QR_CODE_CHECK_URL=""
        COMMENT_HEADER="${{ inputs.comment-header }}"
        PREVIEW_TARGET_FOLDER=""
        PREVIEW_URL=""
        COMMENT_TITLE="${{ inputs.comment-title }}"
        COMMENT_FOOTER="${{ inputs.comment-footer }}"
        UMBRELLA_DIR_INPUT="${{ inputs.umbrella-dir }}"
        UMBRELLA_DIR=""
        WORKFLOW_CONCLUSION=""
        WORKFLOW_EVENT=""
        WORKFLOW_RUN_REPO_FULL_NAME=""
        SERVER_URL="${GITHUB_SERVER_URL:-${{ github.server_url }}}"
        ARTIFACT_UPLOAD_ACTION="v3-node20"

        # Resolve PR context
        if [ "$EVENT_NAME" = "pull_request" ]; then
          HEAD_REPO_FULL_NAME="${{ github.event.pull_request.head.repo.full_name }}"
          PR_NUMBER="${{ github.event.pull_request.number }}"
        elif [ "$EVENT_NAME" = "workflow_run" ]; then
          # Note: workflow_run runs from default branch workflow definition
          HEAD_REPO_FULL_NAME="${{ github.event.workflow_run.head_repository.full_name }}"
          PR_NUMBER="${{ github.event.workflow_run.pull_requests[0].number }}"
          if [ -z "$PR_NUMBER" ] || [ "$PR_NUMBER" = "null" ]; then
            DEPLOYMENT_ACTION="skip"
          fi
          # Read conclusion from the event payload
          WORKFLOW_CONCLUSION="${{ github.event.workflow_run.conclusion }}"
          WORKFLOW_EVENT="${{ github.event.workflow_run.event }}"
          WORKFLOW_RUN_REPO_FULL_NAME="${{ github.event.workflow_run.repository.full_name }}"
        else
          echo "Unsupported event: $EVENT_NAME"
          exit 1
        fi
        # Determine PR type by comparing head repo vs base repo
        if [ "$HEAD_REPO_FULL_NAME" = "$REPO_NAME" ]; then
          PR_TYPE="local"
        else
          PR_TYPE="fork"
        fi

        # Default umbrella dir:
        # - if umbrella-dir is provided: use it
        # - else if target repo is same as current repo (or not set): use "pr-preview"
        # - else: derive from source repo name
        if [ -n "$UMBRELLA_DIR_INPUT" ]; then
          UMBRELLA_DIR="$UMBRELLA_DIR_INPUT"
        else
          # treat empty target-repo as "same repo"
          if [ -z "$TARGET_REPO" ] || [ "$TARGET_REPO" = "$REPO_NAME" ]; then
            UMBRELLA_DIR="pr-preview"
          else
            UMBRELLA_DIR="${REPO_NAME#*/}" # owner/repo -> repo
          fi
          UMBRELLA_DIR="$(printf '%s' "$UMBRELLA_DIR" | tr -cs 'A-Za-z0-9._-' '-')"
        fi

        # Compute paths/URL
        if [ -n "$PR_NUMBER" ] && [ "$PR_NUMBER" != "null" ]; then
          PREVIEW_TARGET_FOLDER="$UMBRELLA_DIR/pr-$PR_NUMBER"
          if [ -n "$PAGES_BASE_URL" ]; then
            PREVIEW_URL="${PAGES_BASE_URL%/}/$PREVIEW_TARGET_FOLDER/"
          else
            PREVIEW_URL="$PREVIEW_TARGET_FOLDER/"
          fi
        fi

        QR_CODE_SUFFIX=""
        QR_CODE_URL=""
        QR_CODE_CHECK_URL=""
        QR_CODE_INPUT="${QR_CODE_INPUT:-true}"

        while IFS='=' read -r key value; do
          case "$key" in
            qr_code_check_url) QR_CODE_CHECK_URL="$value" ;;
            qr_code_url) QR_CODE_URL="$value" ;;
            qr_code_suffix) QR_CODE_SUFFIX="$value" ;;
          esac
        done < <(
          python "$GITHUB_ACTION_PATH/.internal-tools/resolve_qr_comment.py" \
            --server-url "$SERVER_URL" \
            --current-repo "$REPO_NAME" \
            --target-repo "$TARGET_REPO" \
            --target-branch "$TARGET_BRANCH" \
            --preview-target-folder "$PREVIEW_TARGET_FOLDER" \
            --preview-url "$PREVIEW_URL" \
            --qr-enabled "$QR_CODE_INPUT"
        )

        # Decide action
        if [ "$EVENT_NAME" = "pull_request" ]; then
          if [ "$PR_TYPE" = "local" ]; then
            if [ "${{ github.event.action }}" = "closed" ]; then
              DEPLOYMENT_ACTION="remove"
            else
              DEPLOYMENT_ACTION="deploy"
            fi
          else
            if [ "${{ inputs.fork }}" = "true" ]; then
              DEPLOYMENT_ACTION="upload"
            else
              DEPLOYMENT_ACTION="skip"
            fi
          fi
        elif [ "$EVENT_NAME" = "workflow_run" ]; then
          # Only proceed for fork PR workflow_run events that succeeded
          if [ "$WORKFLOW_EVENT" = "pull_request" ] \
            && [ "$WORKFLOW_CONCLUSION" = "success" ] \
            && [ "$HEAD_REPO_FULL_NAME" != "$WORKFLOW_RUN_REPO_FULL_NAME" ]; then
            DEPLOYMENT_ACTION="worker"
          else
            DEPLOYMENT_ACTION="skip"
          fi
        fi

        if [ "$SERVER_URL" = "https://github.com" ]; then
          ARTIFACT_UPLOAD_ACTION="v4"
        fi

        # Expose vars for later steps (single source of truth)
        {
          echo "event_name=$EVENT_NAME"
          echo "pr_type=$PR_TYPE"
          echo "pr_number=$PR_NUMBER"
          echo "deployment_action=$DEPLOYMENT_ACTION"
          echo "artifact_name=$ARTIFACT_NAME"
          echo "meta_file=$META_FILE"
          echo "target_repo=$TARGET_REPO"
          echo "target_branch=$TARGET_BRANCH"
          echo "build_dir=$BUILD_DIR"
          echo "preview_target_folder=$PREVIEW_TARGET_FOLDER"
          echo "preview_url=$PREVIEW_URL"
          echo "qr_code_suffix=$QR_CODE_SUFFIX"
          echo "qr_code_url=$QR_CODE_URL"
          echo "qr_code_check_url=$QR_CODE_CHECK_URL"
          echo "comment_header=$COMMENT_HEADER"
          echo "comment_title=$COMMENT_TITLE"
          echo "comment_footer=$COMMENT_FOOTER"
          echo "umbrella_dir=$UMBRELLA_DIR"
          echo "artifact_upload_action=$ARTIFACT_UPLOAD_ACTION"
        } >> "$GITHUB_OUTPUT"

    - name: Print vars
      shell: bash
      run: |
        echo "event_name=${{ steps.setup.outputs.event_name }}"
        echo "pr_type=${{ steps.setup.outputs.pr_type }}"
        echo "pr_number=${{ steps.setup.outputs.pr_number }}"
        echo "deployment_action=${{ steps.setup.outputs.deployment_action }}"
        echo "target_repo=${{ steps.setup.outputs.target_repo }}"
        echo "preview_url=${{ steps.setup.outputs.preview_url }}"
        echo "qr_code_url=${{ steps.setup.outputs.qr_code_url }}"

    - name: Prepare + upload artifact (fork PR -> worker instructions)
      if: steps.setup.outputs.deployment_action == 'upload'
      shell: bash
      run: |
        set -euo pipefail

        # Build payload folder
        rm -rf __pr_preview_payload
        mkdir -p __pr_preview_payload

        # Decide desired worker action based on PR action
        WORKER_ACTION="deploy"
        if [ "${{ github.event.action }}" = "closed" ]; then
          WORKER_ACTION="remove"
        fi

        # Copy build output when available (open/sync) - closed typically has no build
        if [ "$WORKER_ACTION" = "deploy" ] && [ -d "${{ steps.setup.outputs.build_dir }}" ]; then
          cp -R "${{ steps.setup.outputs.build_dir }}/." __pr_preview_payload/
        fi

        if [ "$WORKER_ACTION" = "deploy" ] \
          && [ "${{ inputs.qr-code }}" != "false" ] \
          && [ -n "${{ steps.setup.outputs.preview_url }}" ] \
          && [[ "${{ steps.setup.outputs.preview_url }}" =~ ^https?:// ]]; then
          python "$GITHUB_ACTION_PATH/.internal-tools/generate_qr_svg.py" \
            "${{ steps.setup.outputs.preview_url }}" \
            "__pr_preview_payload/pr-preview-qr.png"
        fi

        # Write meta for workflow_run as JSON
        COMMENT_TITLE="${{ inputs.comment-title }}"
        COMMENT_FOOTER="${{ inputs.comment-footer }}"

        export META_PATH="__pr_preview_payload/${{ steps.setup.outputs.meta_file }}"
        export WORKER_ACTION
        export PR_NUMBER="${{ steps.setup.outputs.pr_number }}"
        export TARGET_REPO="${{ steps.setup.outputs.target_repo }}"
        export TARGET_BRANCH="${{ steps.setup.outputs.target_branch }}"
        export PAGES_BASE_URL="${{ inputs.pages-base-url }}"
        export COMMENT_HEADER="${{ inputs.comment-header }}"
        export QR_CODE="${{ inputs.qr-code }}"
        export UMBRELLA_DIR="${{ steps.setup.outputs.umbrella_dir }}"
        export COMMENT_TITLE
        export COMMENT_FOOTER

        python - <<'PY'
        import json
        import os
        from pathlib import Path

        Path(os.environ["META_PATH"]).write_text(
            json.dumps(
                {
                    "worker_action": os.environ["WORKER_ACTION"],
                    "pr_number": os.environ["PR_NUMBER"],
                    "target_repo": os.environ["TARGET_REPO"],
                    "target_branch": os.environ["TARGET_BRANCH"],
                    "pages_base_url": os.environ["PAGES_BASE_URL"],
                    "comment_header": os.environ["COMMENT_HEADER"],
                    "qr_code": os.environ["QR_CODE"],
                    "umbrella_dir": os.environ["UMBRELLA_DIR"],
                    "comment_title": os.environ["COMMENT_TITLE"],
                    "comment_footer": os.environ["COMMENT_FOOTER"],
                }
            ),
            encoding="utf-8",
        )
        PY

        echo "${{ steps.setup.outputs.pr_number }}" > "__pr_preview_payload/pr_number.txt"

    - name: Prepare artifact upload helper actions
      if: steps.setup.outputs.deployment_action == 'upload'
      shell: bash
      run: |
        set -euo pipefail

        HELPER_ROOT="$GITHUB_WORKSPACE/.pr-preview-action-helpers"
        rm -rf "$HELPER_ROOT"
        mkdir -p "$HELPER_ROOT"

        cp -R "${{ github.action_path }}/.internal-actions/upload-artifact-v4" "$HELPER_ROOT/"
        cp -R "${{ github.action_path }}/.internal-actions/upload-artifact-v3-node20" "$HELPER_ROOT/"

    - name: Upload artifact (v4)
      if: steps.setup.outputs.deployment_action == 'upload' && steps.setup.outputs.artifact_upload_action == 'v4'
      uses: ./.pr-preview-action-helpers/upload-artifact-v4
      with:
        name: ${{ steps.setup.outputs.artifact_name }}
        path: __pr_preview_payload

    - name: Upload artifact (v3-node20)
      if: steps.setup.outputs.deployment_action == 'upload' && steps.setup.outputs.artifact_upload_action == 'v3-node20'
      uses: ./.pr-preview-action-helpers/upload-artifact-v3-node20
      with:
        name: ${{ steps.setup.outputs.artifact_name }}
        path: __pr_preview_payload

    - name: Checkout (worker)
      if: steps.setup.outputs.deployment_action == 'worker'
      uses: actions/checkout@v4
      with:
        clean: false

    - name: Download artifact (worker)
      if: steps.setup.outputs.deployment_action == 'worker'
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.token != '' && inputs.token || github.token }}
        ARTIFACT_NAME: pr-preview
      run: |
        set -euo pipefail

        if [ -z "${GITHUB_TOKEN:-}" ]; then
          echo "token input is required for fork deploy."
          exit 1
        fi

        API_URL="${GITHUB_API_URL:-https://api.github.com}"
        RUN_ID="${{ github.event.workflow_run.id }}"
        SOURCE_REPO="${{ github.event.workflow_run.repository.full_name }}"

        echo "Fetching artifacts for run $RUN_ID from $SOURCE_REPO via $API_URL"

        resp="$(curl -fsSL \
          -H "Authorization: Bearer $GITHUB_TOKEN" \
          -H "Accept: application/vnd.github+json" \
          "$API_URL/repos/$SOURCE_REPO/actions/runs/$RUN_ID/artifacts")"

        archive_url="$(python -c 'import sys, json, os
        d=json.load(sys.stdin)
        name=os.environ["ARTIFACT_NAME"]
        for a in d.get("artifacts", []):
          if a.get("name")==name:
            print(a.get("archive_download_url",""))
            break
        ' <<< "$resp")"

        if [ -z "$archive_url" ]; then
          echo "Artifact '$ARTIFACT_NAME' not found in workflow run $RUN_ID"
          echo "$resp"
          exit 1
        fi

        rm -rf preview artifact.zip
        mkdir -p preview

        echo "Downloading artifact from $archive_url"
        curl -fsSL -L \
          -H "Authorization: Bearer $GITHUB_TOKEN" \
          -H "Accept: application/vnd.github+json" \
          -o artifact.zip \
          "$archive_url"

        unzip -q artifact.zip -d preview

    - name: Read worker instructions (worker)
      id: worker
      if: steps.setup.outputs.deployment_action == 'worker'
      shell: bash
      run: |
        set -euo pipefail

        META="preview/${{ steps.setup.outputs.meta_file }}"
        if [ ! -f "$META" ]; then
          echo "Missing meta file: $META"
          exit 1
        fi

        while IFS='=' read -r -d '' key value; do
          case "$key" in
            worker_action) WORKER_ACTION="$value" ;;
            pr_number) PR_NUMBER="$value" ;;
            target_repo) TARGET_REPO="$value" ;;
            target_branch) TARGET_BRANCH="$value" ;;
            pages_base_url) PAGES_BASE_URL="$value" ;;
            comment_header) COMMENT_HEADER="$value" ;;
            qr_code) QR_CODE="$value" ;;
            umbrella_dir) UMBRELLA_DIR="$value" ;;
            comment_title) COMMENT_TITLE="$value" ;;
            comment_footer) COMMENT_FOOTER="$value" ;;
          esac
        done < <(
          python - "$META" <<'PY'
        import json
        import sys

        data = json.load(open(sys.argv[1], encoding="utf-8"))
        for key in (
            "worker_action",
            "pr_number",
            "target_repo",
            "target_branch",
            "pages_base_url",
            "comment_header",
            "qr_code",
            "umbrella_dir",
            "comment_title",
            "comment_footer",
        ):
            sys.stdout.write(f"{key}={data.get(key, '')}\0")
        PY
        )

        UMBRELLA_DIR="${UMBRELLA_DIR:-}"
        PREVIEW_TARGET_FOLDER="$UMBRELLA_DIR/pr-$PR_NUMBER"

        if [ -n "${PAGES_BASE_URL:-}" ]; then
          PREVIEW_URL="${PAGES_BASE_URL%/}/$PREVIEW_TARGET_FOLDER/"
        else
          PREVIEW_URL="$PREVIEW_TARGET_FOLDER/"
        fi

        SERVER_URL="${GITHUB_SERVER_URL:-${{ github.server_url }}}"

        QR_CODE_SUFFIX=""
        QR_CODE_URL=""
        QR_CODE_CHECK_URL=""
        QR_CODE_INPUT="${QR_CODE:-true}"
        CURRENT_REPO="${{ github.repository }}"

        while IFS='=' read -r key value; do
          case "$key" in
            qr_code_check_url) QR_CODE_CHECK_URL="$value" ;;
            qr_code_url) QR_CODE_URL="$value" ;;
            qr_code_suffix) QR_CODE_SUFFIX="$value" ;;
          esac
        done < <(
          python "$GITHUB_ACTION_PATH/.internal-tools/resolve_qr_comment.py" \
            --server-url "$SERVER_URL" \
            --current-repo "$CURRENT_REPO" \
            --target-repo "${TARGET_REPO:-}" \
            --target-branch "$TARGET_BRANCH" \
            --preview-target-folder "$PREVIEW_TARGET_FOLDER" \
            --preview-url "$PREVIEW_URL" \
            --qr-enabled "$QR_CODE_INPUT"
        )

        {
          echo "deployment_action=$WORKER_ACTION"
          echo "pr_number=$PR_NUMBER"
          echo "target_repo=$TARGET_REPO"
          echo "resolved_target_repo=${TARGET_REPO:-${{ github.repository }}}"
          echo "target_branch=$TARGET_BRANCH"
          echo "preview_target_folder=$PREVIEW_TARGET_FOLDER"
          echo "preview_url=$PREVIEW_URL"
          echo "comment_header=${COMMENT_HEADER:-pr-preview}"
          echo "qr_code_suffix=$QR_CODE_SUFFIX"
          echo "qr_code_url=$QR_CODE_URL"
          echo "qr_code_check_url=$QR_CODE_CHECK_URL"
          echo "umbrella_dir=${UMBRELLA_DIR:-}"
          echo "comment_title=${COMMENT_TITLE:-PR Preview}"
          echo "comment_footer=${COMMENT_FOOTER:-}"
        } >> "$GITHUB_OUTPUT"

    - name: Generate QR code asset (deploy)
      if: steps.setup.outputs.deployment_action == 'deploy' && inputs.qr-code != 'false' && startsWith(steps.setup.outputs.preview_url, 'http')
      shell: bash
      run: |
        set -euo pipefail

        python "$GITHUB_ACTION_PATH/.internal-tools/generate_qr_svg.py" \
          "${{ steps.setup.outputs.preview_url }}" \
          "${{ steps.setup.outputs.build_dir }}/pr-preview-qr.png"

    - name: Deploy preview
      if: steps.setup.outputs.deployment_action == 'deploy' || steps.worker.outputs.deployment_action == 'deploy'
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        token: ${{ inputs.token != '' && inputs.token || github.token }}
        repository-name: ${{ steps.worker.outputs.resolved_target_repo != '' && steps.worker.outputs.resolved_target_repo || steps.setup.outputs.target_repo != '' && steps.setup.outputs.target_repo || github.repository }}
        branch: ${{ steps.setup.outputs.target_branch || steps.worker.outputs.target_branch }}
        folder: ${{ steps.setup.outputs.build_dir != '' && steps.setup.outputs.deployment_action == 'deploy' && steps.setup.outputs.build_dir || 'preview' }}
        target-folder: ${{ steps.setup.outputs.preview_target_folder != '' && steps.setup.outputs.preview_target_folder || steps.worker.outputs.preview_target_folder }}
        clean: false

    - name: Wait for preview URL
      if: steps.setup.outputs.deployment_action == 'deploy' || steps.worker.outputs.deployment_action == 'deploy'
      shell: bash
      run: |
        set -euo pipefail

        python "$GITHUB_ACTION_PATH/.internal-tools/wait_for_asset.py" \
          --url "${{ steps.setup.outputs.preview_url != '' && steps.setup.outputs.preview_url || steps.worker.outputs.preview_url }}" \
          --attempts "${{ inputs.wait-poll-attempts }}" \
          --interval-seconds "${{ inputs.wait-poll-interval-seconds }}" \
          --label "preview"

    - name: Wait for QR asset (public GitHub only)
      id: qr_health_public
      if: (steps.setup.outputs.deployment_action == 'deploy' || steps.worker.outputs.deployment_action == 'deploy') && inputs.qr-code != 'false' && github.server_url == 'https://github.com'
      continue-on-error: true
      shell: bash
      run: |
        set -euo pipefail

        python "$GITHUB_ACTION_PATH/.internal-tools/wait_for_asset.py" \
          --url "${{ steps.setup.outputs.qr_code_check_url != '' && steps.setup.outputs.qr_code_check_url || steps.worker.outputs.qr_code_check_url }}" \
          --attempts "${{ inputs.qr-wait-poll-attempts }}" \
          --interval-seconds "${{ inputs.qr-wait-poll-interval-seconds }}" \
          --label "qr asset" \
          --expect png

    - name: Resolve QR code suffix
      id: asset_wait
      if: steps.setup.outputs.deployment_action == 'deploy' || steps.worker.outputs.deployment_action == 'deploy'
      shell: bash
      env:
        DEFAULT_QR_CODE_SUFFIX: ${{ steps.worker.outputs.qr_code_suffix != '' && steps.worker.outputs.qr_code_suffix || steps.setup.outputs.qr_code_suffix }}
        QR_CODE_ENABLED: ${{ inputs.qr-code }}
        PUBLIC_QR_CHECK_OUTCOME: ${{ steps.qr_health_public.outcome }}
        SERVER_URL: ${{ github.server_url }}
      run: |
        set -euo pipefail

        FINAL_QR_CODE_SUFFIX="$DEFAULT_QR_CODE_SUFFIX"

        if [ "${QR_CODE_ENABLED:-false}" = "false" ]; then
          FINAL_QR_CODE_SUFFIX=""
        elif [ "$SERVER_URL" != "https://github.com" ]; then
          echo "Skipping runner-side QR validation on non-public GitHub."
          echo "Keeping QR image in comment because access-controlled Pages may redirect the runner to login."
        elif [ "${PUBLIC_QR_CHECK_OUTCOME:-success}" != "success" ]; then
          echo "Public QR health check did not pass. Continuing without QR image in comment."
          FINAL_QR_CODE_SUFFIX=""
        fi

        {
          echo "qr_code_suffix<<EOF"
          printf '%s\n' "$FINAL_QR_CODE_SUFFIX"
          echo "EOF"
        } >> "$GITHUB_OUTPUT"

    - name: Compute comment timestamp (UTC)
      id: comment_time
      if: steps.setup.outputs.deployment_action == 'deploy' || steps.worker.outputs.deployment_action == 'deploy'
      shell: bash
      run: |
        echo "utc=$(date -u '+%Y-%m-%d %H:%M UTC')" >> "$GITHUB_OUTPUT"

    - name: Update sticky PR comment (deploy)
      if: steps.setup.outputs.deployment_action == 'deploy' || steps.worker.outputs.deployment_action == 'deploy'
      uses: marocchino/sticky-pull-request-comment@v2
      with:
        header: ${{ steps.setup.outputs.comment_header != '' && steps.setup.outputs.comment_header || steps.worker.outputs.comment_header }}
        number: ${{ steps.setup.outputs.pr_number != '' && steps.setup.outputs.pr_number || steps.worker.outputs.pr_number }}
        message: |
          | **${{ steps.setup.outputs.comment_title != '' && steps.setup.outputs.comment_title || steps.worker.outputs.comment_title }}** |
          | :---: |
          | ${{ steps.asset_wait.outputs.qr_code_suffix }} 🚀 **View preview at:** <br> ${{ steps.setup.outputs.preview_url != '' && steps.setup.outputs.preview_url || steps.worker.outputs.preview_url }} <br><br> |
          | Built to branch [`${{ steps.setup.outputs.target_branch != '' && steps.setup.outputs.target_branch || steps.worker.outputs.target_branch }}`](${{ github.server_url }}/${{ steps.worker.outputs.resolved_target_repo != '' && steps.worker.outputs.resolved_target_repo || steps.setup.outputs.target_repo != '' && steps.setup.outputs.target_repo || github.repository }}/tree/${{ steps.setup.outputs.target_branch != '' && steps.setup.outputs.target_branch || steps.worker.outputs.target_branch }}) at ${{ steps.comment_time.outputs.utc }}. |
          | ${{ steps.setup.outputs.comment_footer != '' && steps.setup.outputs.comment_footer || steps.worker.outputs.comment_footer }} |
        GITHUB_TOKEN: ${{ inputs.token != '' && inputs.token || github.token }}

    - name: Checkout target repo (remove)
      if: steps.setup.outputs.deployment_action == 'remove' || steps.worker.outputs.deployment_action == 'remove'
      uses: actions/checkout@v4
      with:
        repository: ${{ steps.worker.outputs.resolved_target_repo != '' && steps.worker.outputs.resolved_target_repo || steps.setup.outputs.target_repo != '' && steps.setup.outputs.target_repo || github.repository }}
        ref: ${{ steps.setup.outputs.target_branch != '' && steps.setup.outputs.target_branch || steps.worker.outputs.target_branch }}
        token: ${{ inputs.token != '' && inputs.token || github.token }}
        fetch-depth: 0

    - name: Remove preview directory (remove)
      if: steps.setup.outputs.deployment_action == 'remove' || steps.worker.outputs.deployment_action == 'remove'
      shell: bash
      run: |
        set -euo pipefail

        PREVIEW_DIR="${{ steps.setup.outputs.preview_target_folder != '' && steps.setup.outputs.preview_target_folder || steps.worker.outputs.preview_target_folder }}"
        TARGET_BRANCH="${{ steps.setup.outputs.target_branch != '' && steps.setup.outputs.target_branch || steps.worker.outputs.target_branch }}"
        PR_NUMBER="${{ steps.setup.outputs.pr_number != '' && steps.setup.outputs.pr_number || steps.worker.outputs.pr_number }}"

        if [ -z "$PREVIEW_DIR" ] || [ "$PREVIEW_DIR" = "null" ]; then
          echo "preview_target_folder is not set."
          exit 1
        fi

        echo "Removing: $PREVIEW_DIR"
        rm -rf "$PREVIEW_DIR" || true

        if git status --porcelain | grep . >/dev/null 2>&1; then
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: remove pr-preview ($PREVIEW_DIR) for PR #$PR_NUMBER on $TARGET_BRANCH" || true
          git push origin "$TARGET_BRANCH"
        else
          echo "Nothing to commit."
        fi

    - name: Update sticky PR comment (remove)
      if: steps.setup.outputs.deployment_action == 'remove' || steps.worker.outputs.deployment_action == 'remove'
      uses: marocchino/sticky-pull-request-comment@v2
      with:
        header: ${{ steps.setup.outputs.comment_header != '' && steps.setup.outputs.comment_header || steps.worker.outputs.comment_header }}
        number: ${{ steps.setup.outputs.pr_number != '' && steps.setup.outputs.pr_number || steps.worker.outputs.pr_number }}
        message: |
          | **${{ steps.setup.outputs.comment_title != '' && steps.setup.outputs.comment_title || steps.worker.outputs.comment_title }}** |
          | :---: |
          | 🗑️ This PR has been closed. The preview has been removed. |
          | ${{ steps.setup.outputs.comment_footer != '' && steps.setup.outputs.comment_footer || steps.worker.outputs.comment_footer }} |
        recreate: true
        GITHUB_TOKEN: ${{ inputs.token != '' && inputs.token || github.token }}
