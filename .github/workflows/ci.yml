name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    runs-on:
      - ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Node dependencies
        run: npm ci

      - name: Prettier (check)
        run: npm run prettier:ci

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install yamllint
        run: python -m pip install --upgrade pip yamllint

      - name: yamllint
        run: yamllint -c .yamllint.yml -f parsable .

      - name: actionlint
        uses: devops-actions/actionlint@v0.1.10

      - name: REUSE lint
        uses: fsfe/reuse-action@v6
